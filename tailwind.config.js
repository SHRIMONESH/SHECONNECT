/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{js,jsx,ts,tsx}'], // Add all file extensions where Tailwind classes are used
    theme: {
      extend: {}, // Extend Tailwind's default styles if needed
    },
    plugins: [], // Add Tailwind plugins here if required
  };
  